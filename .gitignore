.gradle/
build/
local.properties
*.iml
.idea/
harvester/output/
.DS_Store
*.hprof

# Claude Code — settings/locks locales de esta máquina, no del proyecto.
.claude/

# Contexto del proyecto para Claude Code — no se versiona a propósito.
CLAUDE.md

# Firma de release — NUNCA subir esto. El .jks real vive fuera del repo
# (~/.android/art-daily-keystore/), pero se ignoran los patrones igual por las dudas.
keystore.properties
*.jks
*.keystore
